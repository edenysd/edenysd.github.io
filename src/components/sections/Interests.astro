---
import Section from "../Section.astro";

import { interests } from "@cv";
---

<Section className={Astro.props.className} title="Interests">
  <ul class="flex items-center gap-4 md:gap-8">
    {
      interests.map(({ name }) => {
        return (
          <li class="px-2 py-1 drop-shadow-[0_0_4px_rgba(var(--color-button-accent),1)]">
            <p class="text-yellow-800 dark:text-yellow-400">{name}</p>
          </li>
        );
      })
    }
  </ul>
</Section>
